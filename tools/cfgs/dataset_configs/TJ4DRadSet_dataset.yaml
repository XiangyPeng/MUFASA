DATASET: 'TJ4DRadDataset'
DATA_PATH: '../data/TJ4DRadSet_4DRadar'

POINT_CLOUD_RANGE: [0, -39.68, -4, 69.12 , 39.68, 2] # [0, -40, -3, 70.4, 40, 1]
# Make sure the size if the width/height is a 64 step (512, 576, 640...)


DATA_SPLIT: {
    'train': train,
    'test': val
}

INFO_PATH: {
    'train': [kitti_infos_train.pkl],
    'test': [kitti_infos_val.pkl],
}

GET_ITEM_LIST: ["points"]
FOV_POINTS_ONLY: False
DATA_AUGMENTOR:
    DISABLE_AUG_LIST: ['placeholder']
    AUG_CONFIG_LIST:
        - NAME: random_world_flip
          ALONG_AXIS_LIST: ['x']
        
        - NAME: random_world_rotation
          WORLD_ROT_ANGLE: [-0.78539816, 0.78539816]

        - NAME: random_world_scaling
          WORLD_SCALE_RANGE: [0.95, 1.05]


POINT_FEATURE_ENCODING: {
    encoding_type: absolute_coordinates_encoding,
    used_feature_list: ['x', 'y', 'z', 'vr','power'], 
    src_feature_list: ['x', 'y', 'z', 'vr','power'],
}


DATA_PROCESSOR:
    - NAME: mask_points_and_boxes_outside_range
      REMOVE_OUTSIDE_BOXES: True

    - NAME: shuffle_points
      SHUFFLE_ENABLED: {
        'train': True,
        'test': False
      }

    # - NAME: transform_points_to_voxels
    #   VOXEL_SIZE: [0.16, 0.16, 6]  #[0.05, 0.05, 4]   [0.16, 0.16, 0.15]
    #   MAX_POINTS_PER_VOXEL: 10
    #   MAX_NUMBER_OF_VOXELS: {
    #     'train': 16000,
    #     'test': 40000
    #   }
